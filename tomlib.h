#define TRUE 1
#define FALSE 0

//takes a double x and returns the sigmoid function 1/(e^(-x)+1) works for all x
double sigmoid(double x);

void random_case_string(char *c);

char random_case(char c);

int is_vowel(char c);

int power_of_two(int x);
